# URL backend для проксирования /api/* в dev и production (Next.js rewrites)
BACKEND_URL=http://localhost:4000

# Опционально: явный URL API с клиента (обычно не нужен — запросы идут на /api/* того же хоста)
# NEXT_PUBLIC_API_BASE_URL=
